@AGENTS.md
Run every command from the repository root. Run the loop inline; a subagent per campaign or a second worktree is optional and never required. The contract above is already loaded; never `cat` AGENTS.md or the skills again.
