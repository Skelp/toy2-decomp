interface:
  display_name: "Continue Decompilation"
  short_description: "Run reconstruction campaigns"
  default_prompt: "Use $continue-decomp to run Toy Story 2 reconstruction campaigns from the candidate queue."
