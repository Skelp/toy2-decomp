#!/usr/bin/env bash

set -euo pipefail

compiler="$1"
shift

script_dir="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
mapfile -d '' -t arguments < <(python "$script_dir/vc6_compile.py" "$@")

exec "$compiler" "${arguments[@]}"
